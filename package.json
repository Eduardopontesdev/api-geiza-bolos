{
  "type": "module",
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "mongodb": "^6.13.0",
    "multer": "^1.4.5-lts.1",
    "prisma": "^6.3.0"
  },
  "scripts": {
    "start": "node index.js",
    "build": "prisma generate",
    "postinstall": "prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prisma": "^6.3.1"
  }
}
